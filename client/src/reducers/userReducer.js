import * as actionTypes from "../actions/actionTypes";

const initialState = {
  routes: {
    byId: {},
    allIds: []
  }
};

export default function(state = initialState, action) {
  return state;
}
